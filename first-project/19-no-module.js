
// for forget password we need this config user id and token and return it to fronted

// const resetUILink = `${config.reset_pass_ui_link}?id=${user.id}&token=${resetToken} `;


// email ache student kaj hobe auth services so eita change korle student kina admin kina faculty kina dekte hobe so change model

// we need new property into user interface to model

// add faculty email into user services


// node mailer setup 

// https://www.nodemailer.com/

// make global utils and create file sendImage and sendEmail

// id and password sent req.body and tojen get req.headers.authoriaation from postman

// why we need me endpoints

// We need meroutes

// A single student does not see another data

// so we create getme route into user

// install multer for picture

// image ke file hisabe upload korbo 